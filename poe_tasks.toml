[tasks.fmt]
help = "Format and auto-fix lint issues"
sequence = [
  { cmd = "ruff format ." },
  { cmd = "ruff check . --fix" },
]

[tasks.fmt-check]
help = "Check formatting without modifying files"
cmd = "ruff format . --check"

[tasks.lint]
help = "Run lint checks"
sequence = [
  "fmt-check",
  { cmd = "ruff check ." },
]

[tasks.type]
help = "Run static type checks"
cmd = "mypy src"

[tasks.test]
help = "Run tests"
cmd = "pytest tests"

[tasks.verify]
help = "Run the read-only verification suite"
sequence = ["lint", "type", "test"]

[tasks.check]
help = "Alias for the read-only verification suite"
sequence = ["verify"]

[tasks.run]
help = "Run the demo CLI"
cmd = "python -m wallet.cli"

[tasks.secret-scan]
help = "Scan tracked repository files for leaked Gemini API keys"
cmd = "python -m wallet.devtools.secret_scan --scope repo"

[tasks.secret-scan-staged]
help = "Scan staged added lines for leaked Gemini API keys"
cmd = "python -m wallet.devtools.secret_scan --scope staged-added"
